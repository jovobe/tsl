#ifndef TSL_READ_FILE_HPP
#define TSL_READ_FILE_HPP

#include <string>

using std::string;

namespace tsl {

/**
 * @brief Reads the given file into a string.
 */
string read_file(const string& filename);

}

#endif //TSL_READ_FILE_HPP
