#ifndef TSE_READ_FILE_HPP
#define TSE_READ_FILE_HPP

#include <string>

using std::string;

namespace tse {

/**
 * @brief Reads the given file into a string.
 */
string read_file(const string& filename);

}

#endif //TSE_READ_FILE_HPP
